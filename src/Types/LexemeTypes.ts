export enum LexemeType {
    PLUS_OPERATOR, MINUS_OPERATOR,
    MUL_OPERATOR, DIVISION_OPERATOR,
    EQUAL_OPERATOR,
    POWER_OPERATOR,
    LEFT_BRACKET, RIGHT_BRACKET,
    REGULAR_STRING,
    COMA_SEPARATOR,
    FUNCTION,
    EOF
};